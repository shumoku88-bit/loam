# Household evidence graph checkpoint

The cross-capability survey is intentionally separate from a numbered executable observation.

It records the dependency map in `HOUSEHOLD_EVIDENCE_GRAPH.md` and changes the research order before new Practical Core implementation.

## Current finding

The HRA / h-kernel household surface does not currently pressure LOAM toward one object per familiar domain noun.

The reusable semantic pressure instead clusters around:

```text
Actual value flow
Scheduled expectation
Capacity / entitlement flow
Attention evidence

plus

explicit relation provenance
historical routing
temporal coordinates
current query policy
backing topology
```

Most report and Envelope names remain downstream observations.

## Important non-finding

This survey does not establish a universal `Fact`, `Occurrence`, `Relation`, `Plane`, or metadata framework.

A shared data shape is only a candidate for implementation reuse. Semantic authority must remain distinguishable whenever a household answer observes the distinction.

## Next executable pressure

The first cross-domain Alloy experiment should compare Actual and Capacity movement shapes:

```text
erase semantic plane
    -> seek a collision between holdings and capacity answers

retain semantic plane
    -> ask whether one signed Effect algebra determines both answers
```

This should happen before scheduled lifecycle is promoted into practical persistence or UI.

## Current realization

This sequence was subsequently executed and qualified:
- Observation 106 qualified the Actual / Capacity plane separation and shared signed algebra.
- Observation 107 qualified shared historical routing shape.
- Observation 108 qualified Commitment projection from Scheduled + routing + lifecycle.
- Observation 109 qualified the Attention lifecycle boundary.
- Observation 110 qualified the derived double-entry accounting view.
- Observation 111 qualified the Actual Locus routing boundary with occurrence validity.

Practical Slice A (A1 capacity movement boundary and A2 historical Actual routing, Consumption, and Remaining) has since been integrated into `main`. Subsequent directions continue to follow real dogfood pressure and formal qualification rather than an ungrounded expansion.
