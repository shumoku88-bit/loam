namespace Observation166

set_option autoImplicit false

/-- Trusted statement surface for the Comparator negative statement-drift trial. -/
theorem vector_claim :
    ((-100 : Int) = -100) ∧ ((100 : Int) = 100) := by
  sorry

end Observation166
