namespace Observation168

set_option autoImplicit false

/-- Trusted statement surface for the first LOAM Comparator + Nanoda field trial. -/
theorem vector_claim :
    ((-100 : Int) = -100) ∧ ((100 : Int) = 100) := by
  sorry

end Observation168
