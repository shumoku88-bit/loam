import Loam.Application.CorrectionFrontierIndexed

/-!
# Correction frontier public entrance

This module remains the stable import entrance for current production consumers.
The list-based semantic/reference layer and the Phase 3H indexed implementation
are physically separated only as a module-granularity experiment. All public
Correction declarations remain in the existing `Loam.Application` namespace.
-/
