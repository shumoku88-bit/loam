# Build outputs
.lake/
dist-newstyle/

# Local generated, test, qualification, and dogfood material.
# This tree is intentionally outside repository history; CI checks this boundary.
scratch/

# Local interpreter and OS noise
__pycache__/
*.py[cod]
.DS_Store

# Reproducible DRAKON outputs that are not committed golden CI fixtures.
docs/drakon/loam-system-map.drn
docs/drakon/loam-read-path-map.drn
docs/drakon/loam-tui-cli-granularity-audit.drn
